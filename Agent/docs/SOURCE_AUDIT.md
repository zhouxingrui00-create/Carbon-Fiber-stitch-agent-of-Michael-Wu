# 来源审计与待确认事项

## 阶段 3 使用边界核查（2026-09-20）

本阶段没有改变原DOCX、manifest、提取JSON或两份种子规格。来源窗外只提示审查，只有用户明确选用且确认适用的人工设备限制才参与硬性超限阻断；质量目标、描述性厚度示例、来源设计规格均不自动成为该限制。原有A–H问题继续未决。

三个场景配置额外引用的原文仍来自包内D1提取版本，保留声明哈希、提取文件哈希及块位置，未改成当前原件哈希：V幅宽 `D1:p0270`；J工位/机构 `D1:p0297`，机头外形/质量/功率 `p0347–p0349`；双层空间/幅宽 `p0147`、`p0188`，链式与双线/旋梭 `p0147`、`p0166`、`p0167`。配置构建前核对对应原句，缺失时明确失败。这些配置声明不追加为实测参数或确认设备，也不修改原33条种子。

矩形密度引用 `D2:p0032`，适用假设由Python函数另行守护；J针密引用 `D1:p0335`，均匀单排换算仍pending。10Hz转换为600针/min须明确每周期一针。没有新证据解决摆动跨距、机械针位间距或设备机构，不认证可达性/避碰。全部软件测试不构成工艺验证。

## 阶段 2 本地读取核查（2026-09-19）

本轮只读核查实际原件，未修改原 DOCX、清单、提取 JSON 或两份 spec：

- D1 当前原件：`../碳纤维/调研报告与技术报告20260806.docx`，SHA-256 `7ad8d0cd20914c20a708544ebbf4e93ad676b8adc843c3fecc5e0fccd61b51a9`，16,284,504 字节。与下文清单版本 `f97ccd33c4afa780aa76ec441c5d8454e6229cc8b6addd4996f1b9812f04853b` 不同，不能认定同一文件。
- D1 主文档共 356 个段落（含空段）、46 个绘图对象、0 张正文表格。旧 JSON 已记录的 255 个非空段落逐块文字一致；**图片/图中文字/图纸尺寸未解析或核验**，文字一致不能证明图像或文件身份一致。
- D2 当前原件：`../碳纤维/碳纤维复合材料缝合技术类型与关键参数.docx`，SHA-256 `b4e2b13767b3ca2c61fb07c751e3649b2d2043eb2ff62858bbdce62b483701c3`，54,318 字节，与清单一致。读取 60 个正文段落、6 张表格，表格行另设锚点，共 107 个证据块。主文档绘图对象为 0，不据此声称全部附属内容已核验。
- D1 原件和旧 JSON 作为独立快照；D2 用当前原件。种子仍保留原引用哈希。JSON 的 `sha256` 标为原件声明哈希，并另外计算 `extraction_sha256`；不能把声明值当成此次原件核验结果。
- 引用以文档 ID、文件哈希、快照 ID 和块/行 ID 定位；原文与单元格内容不规范化改写。章节来自可识别大纲/编号，推断依据单独标记，无法识别的子节不补造。下文原有页码提示依旧仅作辅助导航。
- 六项软件待确认列表均保持 `pending`，未合并、取交集、选择设备版本或推断无底线自锁的实际机构。人工确认记录单独存储，不解除这些未决事项。

来源文件不可用时会明确报告；已存历史证据可按明确版本查看，当前检索只用本次成功读取的版本。文档正文、域、宏和外部关系不能发出命令、改写参数或请求网络。

## 文件身份与引用

D1：调研报告与技术报告20260806(2).docx；设备调研与三个具体技术方案。
D2：碳纤维复合材料缝合技术类型与关键参数(2).docx；技术综述与初始试验方案，文内日期2026年9月7日。

文件哈希见 sources/manifest.json；正文块/表格见 sources/extracted/。这些 JSON 由本次直接读取用户 DOCX 提取，不包含图片文字。`rendered_page_hint` 对应本次上传资料显示页，仅用于辅助导航；程序引用以文件哈希、章节、块 ID 与原句为准。原图和工程尺寸需重新打开原始文件确认，不能从 JSON 声称已经读过图片。

本包只保存提取文字，不重复打包约16 MB的设备报告和图纸。工程原件由用户自行放在 sources/originals/；不要提交到公开仓库。

## 两份资料的使用分工

D2 的“第四—八部分”用于定义研究参数、记录字段、理论密度和实验模板。D2:p0025、D2:p0060 明确不提供统一标准、最优工艺或真实实验原始数据。

D1 用于三个工程项目的配置和设计指标。除非有可定位的实测记录，软件不能把设计段落变成已验证能力。D1 出现的标准代号仅存原文引用，首版不验证其现行性，不宣称满足法规或安全认证。

## 不可静默合并的问题

### A. J型梁针密与通用针距
D1:p0335，9.3.3，资料第63页：5~15针/cm。
D2:t002，资料第5页：p=3–15 mm。
均匀单排假设下 p=10/n，导出约0.667–2 mm。它可能涉及不同目的或定义，也可能需要校核，但本包不能确定原因。记录 scope_difference_or_unresolved，询问线迹、计数方向、单位与实际可调范围。两者不得直接取交集或择一覆盖。

### B. 双层工艺的三类间距
D1:p0147，9.1.3(6)，资料第33页：“多针针距”100/120/200 mm。
D1 9.1.4(3)，资料第38页：缝纫针距10–30 mm。
D2:t002：p=3–15 mm，s=5–20 mm。
至少要分别保留 p、s 和 mechanical_needle_spacing_mm。100/120/200 mm 的方向、是否对应针位/头间距/轨迹间距需结合实际机构确认。双层固定的10–30 mm是场景窗口，不被通用初始窗口强制覆盖。

### C. 双层线迹不一致
D1:p0147，9.1.3(6)，第33页描述“稳定链式结构”。
D1 9.1.4(3)，第37页明确“不采用单线链式”，描述面线与底线锁结及旋梭。
保留两条原始描述。用户确定设备版本和实际机构前，不自动声明底线需求或生成机构特定工艺建议。

### D. 双层厚度不一致
D1:p0120，9.1.1，第26页：双层缝合总厚度≤5 mm。
D1 9.1.7，第42页：双层总厚度≤6 mm。
另有机头最大缝合深度6 mm。不能把工件厚度、设计上限与针具能力混为一谈。先记录未决，获确认后才能启用对应设备限制。

### E. “无底线自锁”不能自动映射锁式
D1 J型梁章节沿用“小型无底线自锁式缝合头”。
D2 将锁式描述为针线与底线交织，同时区分链式、Tufting、盲缝及运动平台。
保留 custom_unconfirmed 和 raw_mechanism_name。须确认成迹原理、勾线/成环机构与背面操作空间。

### F. 张力对象与速度层级不同，不是冲突
D2 供纱0.5–10 N、细纱0.1–3 N；D1 双层布面20–80 N，J型梁布料张力波动±5 N。分别建字段。
D2 研发起步10–100针/min；D1 双层初始100–500、J型梁300–600/最高600、V型最高800针/min。分别标记初始研究窗口、工程初始窗口和最高设计规格。

### G. 空间、抬脚、精度、强度不能替代验证
D1 双层提及约1200 mm内部预留空间、1500–2000 mm缝合空间及600–1000 mm布幅，描述对象不完全明确。不得择最大值作为有效幅宽。
J机头240×100×65 mm、2.5 kg不够证明机器人负载、R角可达性和避碰。
压脚抬升高度不能代替针刺深度或稳定缝合厚度；±0.05/±0.1重复定位不能代替±0.3线迹误差。
V接缝强度≥原布70%是目标，测量条件还要区分干布、接缝和后续成型件。

### H. 概念描述差异要保留来源
D1 调研中的链式、Tufting、盲缝概括与 D2 更细化说明存在差异。应用结构以用户指定的 D2 分离“线迹/机制、路径、平台”，但不要改写或删除 D1 原句；遇到具体机构以待确认方式展示，而非把综述标签当成设备事实。

## 首轮需补充的信息

实际设备型号和稳定可调范围、可用针线组合、材料体系与铺层、厚度测量状态、背面/支撑可达性、具体力学测试方法和对照匹配、过程传感器位置与校准、各目标的允许损失阈值。缺少这些可以建软件和模板，但不能给出已经验证的最优工艺。
